import 'package:flutter/material.dart';

class CalendarClassCard extends StatelessWidget {
  const CalendarClassCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
