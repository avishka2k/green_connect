import 'package:flutter/material.dart';

// ignore: camel_case_types
class profile_attendance_tab extends StatelessWidget {
  const profile_attendance_tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Tab View 3');
  }
}
